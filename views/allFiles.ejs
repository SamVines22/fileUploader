<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Files</title>
</head>
<body>
    <%-include('header')%>
    <h1>Here are the files!</h1>
    <ul>
        <% files.map((item)=> {%>
            <li>
                Name: <%=item.name%> URL: <%=item.url%> 
                <a href = "/file/<%=item.id%>">More info</a>
         
            </li>
        <%})%>
    </ul>
</body>
</html>